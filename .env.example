# Use 120 for the full demonstration or 10-30 for a quick run.
REPLICATION_DELAY_SECONDS=15
# Port used by the API inside and outside the container by default.
PORT=3001
# Change this if the host port is already in use.
HOST_PORT=3001
